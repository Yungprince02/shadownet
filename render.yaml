services:
  - type: web
    name: shadownet
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 24
      - key: ENABLE_HONEYPOTS
        value: "false"
